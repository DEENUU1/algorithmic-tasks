class Solution:
    def multiply(self, num1: str, num2: str) -> str:
        # Why does this question exist ? 
        # Complex solution for very simple problem
        return str(int(num1) * int(num2))